import styled from 'styled-components'

//import { config } from '../../config'
//const { backgroundColor, backgroundColorDark } = config.header;

export const SocialAddyContainer = styled.div`
  position: relative;
  padding: 1em;
  text-align: center;
  margin: 0 auto;
`